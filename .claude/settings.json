{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Glob|Grep",
        "hooks": [
          {
            "type": "command",
            "command": "[ -f graphify-out/graph.json ] && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"graphify: Knowledge graph exists. Read graphify-out/GRAPH_REPORT.md for god nodes and community structure before searching raw files.\"}}' || true"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(python3 -c \"import sys,json; d=json.load(sys.stdin); print(d.get('file_path',''))\" <<< \"$CLAUDE_TOOL_INPUT\" 2>/dev/null); [[ \"$FILE\" == *src/* ]] && python3 scripts/vault-sync.py \"$FILE\" 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}